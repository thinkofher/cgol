project('cgol', 'c',
  version : '0.1',
  default_options : ['warning_level=3'])

subdir('include')
subdir('src')

executable('cgol',
           sources,
           include_directories : inc,
           install : true)
